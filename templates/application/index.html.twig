{% extends 'base.html.twig' %}

{% block title %}Application index{% endblock %}

{% block body %}
    <h1>Application index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Company</th>
                <th>ContactName</th>
                <th>TypeOfCompany</th>
                <th>Description</th>
                <th>ApplicationDate</th>
                <th>HasResponse</th>
                <th>RelaunchDate</th>
                <th>InterviewResume</th>
                <th>City</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for application in applications %}
            <tr>
                <td>{{ application.id }}</td>
                <td>{{ application.company }}</td>
                <td>{{ application.contactName }}</td>
                <td>{{ application.typeOfCompany }}</td>
                <td>{{ application.description }}</td>
                <td>{{ application.applicationDate ? application.applicationDate|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ application.hasResponse ? 'Yes' : 'No' }}</td>
                <td>{{ application.relaunchDate ? application.relaunchDate|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ application.interviewResume }}</td>
                <td>{{ application.city }}</td>
                <td>
                    <a href="{{ path('application_show', {'id': application.id}) }}">show</a>
                    <a href="{{ path('application_edit', {'id': application.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="11">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('application_new') }}">Create new</a>
{% endblock %}
