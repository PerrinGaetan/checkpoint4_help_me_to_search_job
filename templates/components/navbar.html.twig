<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" id="logo" href="#"><img src="{{ asset('build/images/logo.jpg') }}" alt="logo du site"></a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="#">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="{{ path('search_filter_new') }}">Rechercher</a>
        </li>
        <li class="nav-item">
        {% if app.user %}
          <a class="nav-link" href="#">Mes candidatures</a>
        {% endif %}
        </li>
        <li class="nav-item">
          {% if app.user %}
            <a class="nav-link" href="{{ path('app_logout') }}">Me déconnecter</a>
          {% else %}
            <a class="nav-link" href="{{ path('login') }}">Me connecter</a>
          {% endif %}
        </li>
      </ul>
    </div>
  </div>
</nav>